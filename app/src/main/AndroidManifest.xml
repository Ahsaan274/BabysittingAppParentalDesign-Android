<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.design">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Referal_Activity" />
        <activity android:name=".Activities.applicantDetailActivity" />
        <activity android:name=".Activities.JobInfoEdit_Activity" />
        <activity android:name=".Activities.JobInfo_Activity" />
        <activity android:name=".Activities.Activities_Games" />
        <activity android:name=".Activities.Responsibility_Activity" />
        <activity android:name=".Activities.NewJobLast" />
        <activity android:name=".Activities.RecommendationRateActivity" />
        <activity android:name=".Activities.StartEndActivity" />
        <activity android:name=".Activities.MainActivity"/>

    </application>

</manifest>